{
  "Bookings": "f\" Bookings with id {row['Id']} for Unit id {row['UnitId']}. Original price {row['OriginalPrice']}, agreed price {row['AgreedPrice']}. transferred to deal: {row['TransferredToDeal']}. customer id {row['CustomerId']}. project id {row['ProjectId']}. current stage {row['Stage']}. agent id {row['AgentId']}, manager id {row['ManagerId']}, head of sales id {row['HeadOfSalesId']}, CSO id {row['CSOId']}. layout: {row['Layout']}. broker id {row['BrokerId']}. created by {row['CreatedBy']} on {row['CreationDate']}. is deleted: {row['IsDeleted']}. payment link id {row['PaymentLinkId']}. booking reference id {row['BookingReferenceId']}. drafted amount {row['DraftedAmount']}. \"",

  "PaymentLinks": "f\" Payment Link with id {row['Id']} for amount {row['Amount']} {row['Currency']}. payment type {row['PaymentType']}. URL {row['PaymentUrl']}. status {row['Status']}. expires on {row['ExpiryDate']}. reference no. {row['RefNo']} (payment link reference id {row['PaymentLinkReferenceId']}). project id {row['ProjectId']}, agent id {row['AgentId']}. created by {row['CreatedBy']} on {row['CreationDate']}. is deleted: {row['IsDeleted']}. is splitted: {row['IsSplited']}. remaining amount {row['RemainingAmount']}. service charge % {row['ServiceChargePercentage']}. \""
}