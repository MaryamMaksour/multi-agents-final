# First run uvicorn

>> cd service

1- run agent4-service
>> uvicorn agent4-service.main:app --host 0.0.0.0 --port 8004

2- run agent1-service
>> uvicorn agent1-service.main:app --host 0.0.0.0 --port 8001

3- run agents-service
>> uvicorn agents-service.main:app --host 0.0.0.0 --port 8000


# Second run streamlit
>> cd agents-service
>> streamlit run streamlit_app.py

# gir first time
>> git init
>> git add .
>> git commit -m "second commit"
>> git remote add origin https://github.com/MaryamMaksour/multi-agents-final.git
>> git push -u origin main or git push -u origin master

#git
>> git commit -m "second commit"
>> git push -u origin main or git push -u origin master