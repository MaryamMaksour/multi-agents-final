{
  "Payments": "f\" Payment with id {row['Id']}: receipt name '{row['PaymentReceiptDetailsName']}'. status {row['Status']}. amount {row['Amount']} {row['Currency']}. account {row['Account']}. method {row['PaymentMethod']}. receipt date {row['DateOfReceipt']}. transaction no. {row['ChqDDTTNumber']}. transaction date {row['DateOfTransaction']}. cheque deposit date {row['ChequeDepositDate']}, returned date {row['ChequeReturnedDate']}. deal id {row['DealId']}. received by EV on {row['ReceivedByEVDate']}, transferred to developer on {row['TransferredToDeveloperDate']}, received on {row['ReceivedDate']}. POP reference {row['POPReference']}. form requested: {row['isFormRequested']}. loading: {row['IsLoading']}. payment status {row['PaymentStatus']}. customer id {row['CustomerId']}. created by {row['CreatedBy']} on {row['CreationDate']}. is deleted: {row['IsDeleted']}. \"",

  "InstallmentPlans": "f\" Installment plan with id {row['Id']}: name '{row['Name']}'. priority {row['Priority']}. percentage {row['Percentage']}%. amount {row['Amount']}. due on {row['DueDate']}, paid on {row['PaymentDate']}. settled: {row['Settled']}. status {row['Status']}. paid {row['PaidAmount']}, remaining {row['RemainingAmount']}. deal id {row['DealId']}. account type {row['AccountType']}. created by {row['CreatedBy']} on {row['CreationDate']}. is deleted: {row['IsDeleted']}. \"",

  "InstalmentAdjusted": "f\" Instalment adjustment with id {row['Id']}: type {row['InstalmentType']}. amount {row['Amount']} {row['Currency']}. payment method {row['PaymentMethod']}. payment id {row['PaymentId']}. instalment plan id {row['InstalmentPlanId']}. account type {row['AccountType']}. created by {row['CreatedBy']} on {row['CreationDate']}. is deleted: {row['IsDeleted']}. \"",

  "OnlinePaymentTransactions": "f\" Online payment transaction with id {row['Id']}: transaction no. {row['TransactionNumber']}. amount {row['Amount']}. status {row['Status']}. link {row['PaymentLink']}. created on {row['CreationDateTime']}. business entity id {row['BusinessEntityId']}, type {row['BusinessEntityType']}. \"",

  "PaymentLinks": "f\" Payment Link with id {row['Id']} for amount {row['Amount']} {row['Currency']}. payment type {row['PaymentType']}. URL {row['PaymentUrl']}. status {row['Status']}. expires on {row['ExpiryDate']}. reference no. {row['RefNo']} (payment link reference id {row['PaymentLinkReferenceId']}). project id {row['ProjectId']}, agent id {row['AgentId']}. created by {row['CreatedBy']} on {row['CreationDate']}. is deleted: {row['IsDeleted']}. is splitted: {row['IsSplited']}. remaining amount {row['RemainingAmount']}. service charge % {row['ServiceChargePercentage']}. \"",

  "PaymentPlanDetails": "f\" Payment plan detail with id {row['Id']}: sales proposal id {row['SalesProposalId']}. EV instalment '{row['EVInstalment']}'. due on {row['DueDate']}. amount {row['Amount']}. percentage {row['Percentage']}%. payment term item id {row['PaymentTermItemId']}. created by {row['CreatedBy']} on {row['CreationDate']}. is deleted: {row['IsDeleted']}. \"",

  "PaymentSplits": "f\" Payment split with id {row['Id']}: payment link id {row['PaymentLinkId']}. amount {row['Amount']}. status {row['Status']}. is full payment amount: {row['IsFullPaymentAmount']}. created by {row['CreatedBy']} on {row['CreationDate']}. is deleted: {row['IsDeleted']}. \"",

  "PaymentTerms": "f\" Payment term with id {row['Id']}: PTID {row['PTID']}. name '{row['Name']}'. admin fee {row['AdminFee']}, DLD fee {row['DLDFee']}. exchange rate {row['ExchangeRate']}. active: {row['Status']}. currency {row['Currency']}. bed type {row['BedType']}. created by {row['CreatedBy']} on {row['CreationDate']}. is deleted: {row['IsDeleted']}. \"",

  "PaymentTermItems": "f\" Payment term item with id {row['Id']}: name '{row['Name']}'. gap {row['Gap']}. percentage {row['Percentage']}%. due on {row['DueDate']}. payment term id {row['PaymentTermId']}. milestone gap type {row['MilestoneGapType']}. created by {row['CreatedBy']} on {row['CreationDate']}. is deleted: {row['IsDeleted']}. \"",

  "ProjectPaymentPlans": "f\" Project payment plan with id {row['Id']}: project id {row['ProjectId']}. payment term id {row['PaymentTermId']}. created by {row['CreatedBy']} on {row['CreationDate']}. is deleted: {row['IsDeleted']}. \"",

  "ProjectPaymentTerms": "f\" Project payment term with id {row['Id']}: layout '{row['Layout']}'. project id {row['ProjectId']}. minimum amount {row['MinimumAmount']}. \""
}
